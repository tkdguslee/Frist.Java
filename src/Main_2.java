public class Main_2 {
    public static void main(String[] args) {

        int number = 21;
        Integer num = number;
        System.out.println(num.intValue());
        // 문자형은 기본형
        // 문자(char) -> 기본형 !!이것이 가장 큰 특징!!
        // 문자열은 참조형
        // 문자열(String) -> 참조형 !!이것이 가장 큰 특징!!
        // 문자는 기본적으로 1byte만 사용한다.
        // \0 (널문자)

        // 참조형은 뭔가 복잡하다 그래도 (별도)영역에 저장하고 그 주소를 get해서 그 주소값을 저장한다!!!
        // 참조형 변수 = 주소형 변수!!

        // Stack 영역, Heap 영역

        // 동적 알 수 없을때...
        /*
움직일동!

자바는 기본적으로 Scanner에 in객체가 있다.
행동 == 명령















        */
    }
}
